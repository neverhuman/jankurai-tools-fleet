# Vibe coverage sources

The five source documents are preserved verbatim from `neverhuman/jankurai-paper`
commit `43834faa3bb02481785bcdd410b5cdb3ec7ae586` (initial split extraction).
`agent/vibe-coverage.toml` maps their 260 rows; `ops/ci/tool-adoption.sh` verifies
that mapping with the installed auditor. Keep these inputs committed so a fresh
standalone fleet checkout can replay coverage.
